{
  "manifest_version": 2,

  "name": "YouTube Time URL maker",
  "description": "This extension allows the user to create
  timestamped URLs to automatically redirect to the wanted
  time in the youtube video.",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab",
    "storage"
  ]
}
